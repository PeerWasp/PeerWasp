app.artifact=peerwasp
# form: x.x.x, do not use snapshot here.
#In order to use a snapshot package, change the jarname temporarily to ...-SNAPSHOT.jar
app.version=0.0.2
app.jarname=${app.artifact}-${app.version}.jar
app.jar=${app.build}/${app.jarname}

# FIXME: path name is still peerbox
app.pomfile=peerbox/pom.xml
app.build=peerbox/target

app.winbuild=build/Windows